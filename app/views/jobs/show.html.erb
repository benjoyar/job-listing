<div class="col-md-4 col-md-offset-4">
  <h1> <%= @job.title %> </h1>

  <p>
    <%= simple_format(@job.description) %>

  </p>

  <div class="text-center">
    <%= link_to("投交履历", new_job_resume_path(@job), :style => "font-size: 20px;" , class: 'btn btn-success btn-md') %>
  </div>
</div>
